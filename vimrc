source $VIMRUNTIME/vimrc_example.vim

set langmenu=en_US.UTF-8    " language of the menu (gvim)
language en                 " language of the messages / ui (vim)

set number                  " line numbering

set nobackup

set shell=powershell
